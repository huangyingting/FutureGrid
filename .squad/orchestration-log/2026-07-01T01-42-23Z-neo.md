# Neo — Pulse Page + Integration (Batch 9)

**Timestamp:** 2026-07-01T01:42:23Z  
**Batch:** 9 (Data Richness)  
**Issues:** #35 (Pulse page), #37 (Integration)

## Deliverables

### Labor Market Pulse Page (#35)

**New components (7 files):**
- `app/pulse/page.tsx` — Server component shell
- `app/pulse/layout.tsx` — Metadata (title, OG, Twitter tags)
- `components/pulse/PulseView.tsx` — "use client": hero, 4 stat cards (latest layoffs/openings/hires/quits), trend section, industry section, methodology
- `components/pulse/JoltsTrendChart.tsx` — "use client" D3 area/line (2001–2025 layoffs, NBER recession shading, COVID spike annotation, hires toggle, hover tooltip)
- `components/pulse/JoltsIndustryChart.tsx` — "use client" D3 horizontal bar (layoffs by 19 supersectors, level/rate toggle, hover tooltip)
- `lib/i18n/messages/en/pulse.ts` — 36 English i18n keys
- `lib/i18n/messages/zh/pulse.ts` — 36 Chinese i18n keys (exact mirror)

**i18n namespace:** `pulse` (36 keys, all mirrored en/zh)

**Chart design decisions:**
- Excluded aggregate codes `"000000"` (total nonfarm) and `"100000"` (total private) from industry chart
- Y-axis: JOLTS values in thousands; display SI suffixes (e.g., "2M", "11M")
- COVID peak: Vertical dashed amber line + label via `getJoltsLayoffsPeak()`
- Reduced motion: Entrance animations (path draw, bar slide-in) skip when `prefers-reduced-motion` set
- Accessibility: Both charts `role="img" + aria-label + sr-only` text

### Integration (#37)

**Changes to existing files:**
- `lib/i18n/messages/index.ts` — Added imports for `pulseEn`, `pulseZh`; registered `pulse` namespace
- `components/pulse/PulseView.tsx` — Replaced inline `usePulseT()` with `useT("pulse")`
- `components/pulse/JoltsTrendChart.tsx` — Replaced inline `usePulseT()` with `useT("pulse")`
- `components/pulse/JoltsIndustryChart.tsx` — Replaced inline `usePulseT()` with `useT("pulse")`
- `components/dashboard/Sidebar.tsx` — Added `IconPulse` (activity line glyph); inserted `/pulse` nav entry
- `scripts/smoke-test.mjs` — Added `"/pulse"` to ROUTES array

## Quality Gates

✅ `npm run build` exit 0, route `/pulse` marked as static export (○)  
✅ `npm run lint` exit 0 (all pulse components + nav edits)  
✅ All 121 tests pass (includes pulse render regression test)  
✅ Headless-Chrome smoke test: `/pulse` loads, no error boundary, DOM clean  
✅ Theme-aware D3 charts (isDark in effect deps)  
✅ WCAG AA contrast + focus-visible verified  

## Observations

- D3 charts depend on JOLTS loader (`lib/jolts.ts`), which imports snapshot only for /pulse route chunk
- Translations: full i18n parity (36 keys, en/zh exact match). No localization needed.
- No regression to existing routes; sidebar nav updated consistently (icon + label pair)
