# Switch — Layoffs Page Design & UX (Batch 9)

**Timestamp:** 2026-07-01T01:42:23Z  
**Batch:** 9 (Data Richness)  
**Issue:** #36 (Layoffs page)

## Deliverables

### Recent Mass Layoffs Page

**New components (7 files):**
- `app/layoffs/layout.tsx` — Metadata (title, OG, Twitter tags)
- `app/layoffs/page.tsx` — Server component shell
- `components/layoffs/LayoffsView.tsx` — "use client": hero (CA-only callout), 4 stat cards, trend chart section, top employers section, layoff-type breakdown, searchable table
- `components/layoffs/WarnTrendChart.tsx` — "use client" D3 combo (bars = monthly employees, line = monthly notices, dual y-axes, hover tooltip, theme-aware)
- `lib/i18n/messages/en/layoffs.ts` — 46 English i18n keys
- `lib/i18n/messages/zh/layoffs.ts` — 46 Chinese i18n keys (exact mirror)
- `lib/i18n/useLayoffsT.ts` — Type-safe `useLayoffsT()` hook (temporary; replaced in integration)

**i18n namespace:** `layoffs` (46 keys, all mirrored en/zh)

**Table design:**
- All 1,588 CA WARN notices; searchable by company or county
- Sortable: date (desc) or employees (desc)
- Pagination: 50 rows shown, "show more" adds 50 more (no server round-trip)
- Stable React key: `${company}-${noticeDate ?? "null"}-${index}`
- Accessibility: `<th scope="col">` for column headers

**Honesty & transparency:**
- Hero: "California only — not all 50 states"
- Amber "CA Only" badge prominently displayed
- Source attribution: "California Employment Development Department (EDD)"
- License: "Public Domain (California public record)"
- WARN Act explanation in methodology footnote

**Chart design (WarnTrendChart):**
- D3 scaleBand for months (x), scaleLinear for employees (left Y) + notices (right Y)
- Bars animate from baseline unless `prefers-reduced-motion` set
- Hover overlay: non-hovered bars fade; React state tooltip
- isDark in effect deps (redraws on theme change)
- Accessibility: `role="img" + aria-label + sr-only` summary

## Quality Gates

✅ `npm run build` exit 0, route `/layoffs` marked as static export (○)  
✅ `npm run lint` exit 0 (all layoffs components + new i18n)  
✅ All 121 tests pass (includes layoffs render regression test)  
✅ Headless-Chrome smoke test: `/layoffs` loads, no error boundary, DOM clean  
✅ All 1,588 notices loaded, sortable, searchable, paginated  
✅ Theme-aware D3 combo chart (isDark in effect deps)  
✅ WCAG AA contrast + focus-visible verified; reduced-motion respected  

## Observations

- Chose to label "California only" prominently because WARN Act is state-managed; CA EDD is the only readily available comprehensive state snapshot
- Table client-side search sufficient for 1,588 rows (no server pagination needed)
- D3 combo chart pattern established; matches QuadrantScatterChart (theme-aware, role="img" accessibility)
- useLayoffsT temporary workaround (integration pass replaces with useT("layoffs"))
